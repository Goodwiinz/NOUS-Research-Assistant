# Chat QA — 2026-09-17

The owner narrowed the campaign to chat. Manual browser/API execution completed
at 18:44 UTC against `https://goodwiinz.tech` and
`https://dev-api.gen-text.app`. The curated report contains **23 PASS, 3 FAIL,
1 BLOCKED**. These are final meaningful observations; corrected diagnostic
oracles and earlier out-of-scope research probes are not additional app defects.

## Observed deployment

Backend source: `f733b179c0a23d45bc2f9d71737640203330a4d1`, image digest
`sha256:46678e7240e09f697609c3651b4af06c1b8f125d0e8357557e26d15cbdaab508`.
Frontend source: `e81ab704561fc608791166ab3eeaddda25df2e17`, the values-only
promotion commit. Argo and pod metadata established backend identity; the
public frontend asset deployment ID matched its GitHub Vercel success status.
The generic health version `1.0.0` does not identify the deployed commit.
See the [deployment evidence record](2026-09-17-nous-workflow-qa.md) for timing,
CI evidence, and the corrected rollback procedure.

## Findings and repair plan

| Finding | Live evidence | Repair and verification |
| --- | --- | --- |
| **CHAT-ATTACH-01: attachment does not ground answer** | A synthetic TXT uploaded, reached `indexed`, and its exact ID persisted on the user message. Asked for its explicit launch code, the answer said it could not access the file. | Pass validated IDs into bounded, permission-checked context; keep source provenance and honest unavailable/processing behavior. Add access and no-attachment regression tests. After deployment, repeat both known-fact and absent-fact questions. |
| **CHAT-RESTORE-02: cold reload can hide transcript and pending approval** | Backend awaited confirmation, while `/chat?thread=...` and reload showed an empty transcript with no approval controls. Clicking the same sidebar thread restored them. A later stopped transcript also required sidebar recovery. | Preserve URL selection across asynchronous conversation initialization while respecting newer navigation and `new=1`. Add deferred-response regression tests, then repeat cold reload and pending-approval recovery after deployment. |
| **CHAT-READ-03: transient transcript HTTP 500** | At 18:35:42 UTC, request `da513609ddad8b5e838ca3e9e825de40` failed with asyncpg `ConnectionDoesNotExistError` during a citations SELECT. Read recovered with 200 at 18:35:47. | Unresolved availability follow-up: inspect managed database/pool disconnects. Consider bounded fresh-transaction retry only for idempotent reads if recurrence supports it. Do not blindly retry message writes. |

The first two repairs are being implemented with Luna max and reviewed locally.
They are **not deployed**. Local regression tests cannot establish a repaired
live workflow. The third finding is not claimed fixed.

## Passing coverage and limits

- Exact live-model answer, follow-up recall, and a successful completed-answer
  reload control. The separate cold-restoration failure still applies.
- UI Stop, durable cancelled status, nonempty persisted partial, idle resume,
  repeated Stop, and stale Stop returning 409 while a newer run continued.
- Markdown export and an actually parsed two-page PDF containing the marker.
- Real sidebar draft isolation, history search, Unicode, mobile newline input,
  attachment-control visibility, and no horizontal overflow at 390 × 844.
- Unsupported upload error/removal recovery and supported upload/indexing.
  Successful ingestion is distinct from failed attachment grounding.
- Approval denial after sidebar recovery. No destructive action was approved.
- Authenticated invalid-input rejection, unauthenticated transcript denial,
  message ID idempotency, and exact Unicode round-trip.
- Controlled unsafe Markdown rendering and a single browser connection reset.
  Retry completed through the real model with one user/assistant pair.
- Four public preparation steps displayed and persisted. Provider public
  `reasoning_summary` coverage is BLOCKED: tested turns returned no summary.

One account does not establish cross-tenant isolation. This campaign did not
perform load testing. Known attachment grounding failed, so its absent-fact
follow-up was not run. Controlled transport/content checks are labeled separately
from model behavior; no claim about hidden model reasoning is made.

## Cleanup and reports

All seven remaining exact-owned chat resources were deleted after confirming
four threads had idle resume responses. There are zero retained resources and
zero tracked active runs. Pre-existing account data was not deleted. Earlier
research fixtures had already been removed when scope changed.

Private, sanitized standalone artifacts on the execution host:

- `/tmp/nous-workflow-qa-20260917/chat-manual-report/nous-qa-report.html`
- `/tmp/nous-workflow-qa-20260917/chat-manual-report/nous-qa-report.json`

These artifacts contain only synthetic campaign evidence and version metadata;
credentials and session material are stored separately and are not committed.
The reusable command and its write/cleanup contracts are documented in
[the QA CLI README](../../tests/e2e/qa/README.md).

## Automated campaign and availability amendment, 19:11 UTC

The initial 14-case authenticated API campaign reported 10 PASS, 3 FAIL and
1 BLOCKED, with complete cleanup. Two Q&A assertions failed (missing follow-up
`done`, and an exact-marker mismatch); these require safe event-level diagnostics
before attributing a model, transport, or context-isolation defect. The third
failure was an incorrect tool oracle: the first-mounted message-list route
explicitly returns HTTP 200 with an empty list for missing/inaccessible threads.
That is not evidence of content disclosure. The corrected oracle must reject
nonempty or unknown response shapes while accepting only that empty contract.

The public prerequisite campaign passed all three selected chat prerequisites
after fixing an immediate DOMContentLoaded assertion that raced login rendering.
An independent browser pass completed reload persistence, unsupported upload,
and durable Stop. Other browser failures exposed ambiguous composer selectors
and a transcript comparison including UI metadata; they are harness findings,
not new product defects. A history/draft case timed out and remains unclassified
pending a stage-labeled repeat. Earlier manual passes remain separate evidence.

The Stop case proved `cancelled`, nonempty persisted output, idle resume, and
correct UI after reload. Its CLI cleanup conservatively retained three resources
because a cancelled SSE response closed without `done`. Root rechecked the exact
run as cancelled and the exact thread resume as 204, then deleted those three
ledger-owned resources successfully. The original incomplete report remains
honest; `cli-stop-cleanup-recovery.json` records the subsequent resolution.

**Availability observation:** Kubernetes recorded five readiness timeouts and
two liveness timeouts between 19:06:17 and 19:06:52 UTC. A read-only deployment
snapshot briefly showed zero ready API replicas; the same pod then returned
ready with zero restarts, and public health/readiness returned 200. Backend
digest and frontend deployment remain unchanged. This is additional unresolved
availability evidence, not proof that the earlier database disconnect caused
it. No rollout or cluster mutation was performed during this campaign.

Two private browser reports initially included unrelated sidebar labels inside
Playwright strict-selector errors. Those snippets were removed from both JSON
and HTML while retaining failure status, category, and cleanup evidence. The
runner is being hardened to emit safe browser error categories automatically.
